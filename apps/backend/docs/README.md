# Backend Endpoint Docs

- POST /auth/register - Create an email/password Identity user and return an API JWT.
- POST /auth/login - Authenticate an email/password Identity user and return an API JWT.
- POST /auth/google - Validate a Google ID token, create or link the Identity user, and return an API JWT.
